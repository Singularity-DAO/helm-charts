apiVersion: v1
kind: Service
metadata:
  name: {{ include "msd-fastapi.fullname" . }}
  labels:
    {{- include "msd-fastapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "msd-fastapi.selectorLabels" . | nindent 4 }}
